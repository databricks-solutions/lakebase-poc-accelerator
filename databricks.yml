# This is a Databricks asset bundle definition for lakebase_accelerator.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
# Do not change the bundle name or uuid.
bundle:
  name: lakebase_accelerator
  uuid: 5fec423c-234a-4869-a92b-766f76f6f70f

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://e2-demo-field-eng.cloud.databricks.com

  # prod:
  #   mode: production
  #   workspace:
  #     host: https://e2-demo-field-eng.cloud.databricks.com
  #     # We explicitly deploy to /Workspace/Users/anhhoang.chu@databricks.com to make sure we only have a single copy.
  #     root_path: /Workspace/Users/anhhoang.chu@databricks.com/.bundle/${bundle.name}/${bundle.target}
  #   permissions:
  #     - user_name: anhhoang.chu@databricks.com
  #       level: CAN_MANAGE

